---

java_home: /etc/alternatives/java_sdk

# When using custom host names, so the servers are resilient to changing ip addresses
# https://ambari.apache.org/1.2.3/installing-hadoop-using-ambari/content/ambari-chap7a.html
manual_registration: no

# paramaters passed to the various ambari roles
ambari:

  # ambari API vars
  stack_name : HDP
  stack_version : 2.2

  # blueprint & cluster creation
  blueprint_name : example
  cluster_name : serenity

  # hadoop configuration
  default_password : hadoop
  hive_user : hive
  hive_pass : supersecret
  nagios_contact : root@localhost

####################
# don't touch this unless you know what you're doing
  ambari_server: "{{ hostvars[groups['ambari'][0]]['hostname'] }}"
#  ambari_server: "{{ hostvars[groups['ambari'][0]]['ansible_ssh_host'] }}"
